import styled from "styled-components";

const StyledSection = styled.section`
  /* mobile */

  /* mobile large */
  @media (480px <= width) {
  }

  /* tablet */
  @media (768px <= width) {
  }

  /* laptop */
  @media (960px <= width) {
  }

  /* desktop */
  @media (1400px <= width) {
  }
`;

export default StyledSection;
